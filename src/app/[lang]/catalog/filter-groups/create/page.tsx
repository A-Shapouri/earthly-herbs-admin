'use client';
import React from 'react';
import Breadcrumbs from '@elements/breadcrumbs';
import Div from '@elements/div';
import Text from '@elements/text';
import FilterGroupDetails from '../components/filter-group-details';

const NewFilterGroup = () => {
  return (
    <Div className={'flex-col justify-center w-full gap-4 md:gap-8'}>
      <Div className={'w-full justify-between md:flex-row flex-col gap-4'}>
        <Breadcrumbs
          breadcrumbsData={[
            {
              label: 'Filter Groups',
              path: '/en/catalog/filter-groups',
            },
            {
              label: 'Create',
            },
          ]}
        />
        <Text className='whitespace-nowrap' type={'bold'} typography={['xl', 'xl']}>Add New Filter Group</Text>
      </Div>
      <FilterGroupDetails />
    </Div>
  );
};

export default NewFilterGroup;
